# PRECHECK v44

## Проверено перед выдачей

- Структура файлов возвращена к прежней: иконки лежат в корне репозитория.
- `manifest.webmanifest` ссылается на `./icon-192.png` и `./icon-512.png`.
- `service-worker.js` кэширует иконки из корня.
- `index.html` обновлён до версии `v44-root-icons`.
- Папка `icons/` не используется.
- Сохранены функции v43:
  - кадастровый номер;
  - координаты;
  - ссылка на Яндекс.Карты;
  - общее фото здания;
  - рекомендации в начале отчёта;
  - подсказки `?` по блокам.
