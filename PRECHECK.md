# PRECHECK v48

- База: v47 service life safe.
- Новые поля встроены в `newBuilding`, `building`, `renderPassport`.
- Отчёт обновлён через прямую замену `buildBuildingReport`.
- `node --check`: OK.
- Структура файлов в корне.
